# Personal Work Bot Working Agreement

This directory is currently a coordination workspace rather than a detected
application repository. Follow the global Codex rules in `/Users/mac/.codex/AGENTS.md`.

## Local Rules

- Treat new app folders inside this workspace as separate projects unless the
  user says they are one repo.
- When a concrete project is added, create or update its project-level
  `AGENTS.md` from `/Users/mac/.codex/templates/AGENTS.project.md`.
- Do not assume a package manager or framework until project files are present.
- Prefer small, reversible changes and document verification clearly.
